[pytest]
asyncio_mode = auto
timeout = 90
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    slow: tests that may take > 30 seconds
    requires_stack: tests that require the full Docker stack to be running
