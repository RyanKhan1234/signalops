# Traceability Store — Environment Variables

# PostgreSQL async connection URL
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/signalops

# Connection pool settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# Server settings
API_PORT=8002
LOG_LEVEL=INFO
